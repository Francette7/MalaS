<%= form_with(model: club, local: true) do |form| %>
  <% if club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
        <% club.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
     <%= form_tag url_for(action: 'create'), method: "club" do %>
<input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >

   <div class="field">
    <%= label_tag :name_club %>
    <%= text_area_tag :nom_club, @club.nom_club %>
  </div>


   <div class="field">
    <%= label_tag :categorie %>
    <%= text_area_tag :categorie, @club.categorie %>
  </div>

 

<div class="form-group">
   <label>Quarter</label>
   <%=select_tag 'quarter_id', options_for_select(@quarters.collect{ |t| [t.name, t.id] }), class:'form-control'%>
 </div>


  <div class="actions">
     <%= submit_tag "Create club" %>
  </div>
<% end %>

